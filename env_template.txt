# Environment Variables Template
# Copy this file to .env and fill in your actual values

# Google Maps API Key
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY_HERE

# OpenWeatherMap API Key (for weather data in aurora forecast)
OPENWEATHER_API_KEY=YOUR_OPENWEATHER_API_KEY_HERE

# Google Drive API (using Application Default Credentials)
# No service account keys needed - uses secure OAuth tokens
# For development: Run 'gcloud auth application-default login'
# For production: Configure Workload Identity Federation

# Firebase Configuration (if needed)
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_API_KEY=your-firebase-api-key

# Other API Keys (if needed)
# BOKUN_API_KEY=your-bokun-api-key
# BOKUN_SECRET=your-bokun-secret

# Note: Never commit the actual .env file to version control
# The .env file is already in .gitignore 